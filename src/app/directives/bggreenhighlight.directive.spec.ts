import { BggreenhighlightDirective } from './bggreenhighlight.directive';

describe('BggreenhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BggreenhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
